[project]
name = "flockworld"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "jax>=0.9.0.1 ; sys_platform == 'win32'",
    "jax[cuda12]>=0.9.0.1 ; sys_platform != 'win32'",
    "opencv-python>=4.13.0.92",
]
